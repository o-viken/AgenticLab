name: React frontend

on:
  pull_request:
    paths:
      - 'src/AgenticLab.React/**'
      - 'src/AgenticLab.Bff/**'
      - 'src/AgenticLab.ServiceDefaults/**'
      - 'src/AgenticLab.AiService/**'
      - 'tests/AgenticLab.Bff.Tests/**'
      - '.github/workflows/react-frontend.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: src/AgenticLab.React/package-lock.json
      - run: dotnet test tests/AgenticLab.Bff.Tests/AgenticLab.Bff.Tests.csproj
      - run: npm ci
        working-directory: src/AgenticLab.React
      - run: npm run lint && npm run typecheck && npm test && npm run build
        working-directory: src/AgenticLab.React
      - run: npx playwright install --with-deps chromium
        working-directory: src/AgenticLab.React
      - run: npm run test:e2e
        working-directory: src/AgenticLab.React
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: react-browser-results
          path: src/AgenticLab.React/test-results